
---

# DOKKAEBI / INVENTORY  
React 기반 드론 재고·입출고 관리 콘솔

> 해시 라우팅 기반 정적 웹앱 · Firebase RTDB 연동 · GitHub Pages 배포  
> 앱 헤더에 `DOKKAEBI/INVENTORY vX.Y.Z` 형식으로 버전 노출 (환경변수/로컬 라벨)

[▶️ 라이브 데모 바로가기](https://x2okmiin.github.io/dokkaebi-inventory/#/) · 문의: **gwdokkebinv@gmail.com**

---

## 📌 프로젝트 메타정보

- **Maintainer**: 최석민 (강원도립대학교 드론융합과 2학년 · 25년도 학회장)  
- **Club**: 도깨비드론축구단  
- **Purpose**: 동아리 물품/재고 정리를 효율적으로 관리하고,  
  차후 다른 부원들이 **이어받아 몇 년간 유지 가능한 콘솔**로 발전시키기 위함  
- **Status**: 현재 1인 진행, 추후 동아리 부원 인수인계 예정  
- **Release Track**: `1.6.0-beta.x` 안정화 진행 중 (정식 1.6.0 전 단계)  

---

## 목차
- [소개](#소개)
- [핵심 기능](#핵심-기능)
- [데이터 스키마](#데이터-스키마)
- [빠른 시작](#빠른-시작)
- [Firebase 설정](#firebase-설정)
- [빌드 & 배포](#빌드--배포)
- [테스트 체크리스트](#테스트-체크리스트)
- [릴리즈 게이트](#릴리즈-게이트)
- [장애 · 디버깅](#장애--디버깅)
- [FAQ](#faq)
- [운영 원칙](#운영-원칙)
- [변경 이력](#변경-이력)
- [라이선스](#라이선스)

---

## 소개
**DOKKAEBI-INVENTORY**는 드론 동아리/팀 운영을 위한 **재고 관리 + 입·출고 기록** 콘솔이야.  
브라우저에서 동작하는 **정적 웹앱**이고, **Firebase RTDB**랑 연동해서 여러 명이 동시에 접속해도 실시간 동기화돼.

- 배포: **GitHub Pages** (`/#/` 해시 라우팅)
- 저장: **LocalStorage + Firebase RTDB** 동시 사용
- 버전: 헤더에 `REACT_APP_VERSION` 노출, 필요 시 로컬 라벨 더블클릭으로 수정 가능

---

## 핵심 기능
- **재고 관리 (CRUD)**  
  - 장소/카테고리/품목 단위로 관리  
  - 검색 및 상세 팝업 지원
- **입·출고 기록 페이지 (`/#/logs`)**  
  - 필터(날짜/장소/품목), CSV/Excel 내보내기  
  - 동일 키(장소+품목+입/출) 1시간 이내는 자동 병합
- **검색 → 위치 이동**  
  - 결과 클릭 시 해당 품목 위치로 스크롤, 필요한 `details` 자동 오픈
- **반응형 UI**  
  - 720–1099px: 카드 2×2  
  - ≥1100px: 3열 + “전체” 카드 가운데 고정  
  - 모바일: 장소 카드 세로 스크롤 유지, 헤더 우측 로그아웃 버튼 보장
- **상세 팝업 UX**  
  - 팝업 열릴 때 내부 `details`를 모두 펼쳐서 보기
- **관리자/로그인**  
  - 로그인 화면 → 학번 + 이름 + 비밀번호(`2513`) 입력  
  - 관리자 모드에서만 수정/삭제/추가 버튼 노출  
  - 10분 무활동 시 자동 로그아웃
- **📤 재고 Excel 내보내기**  
  - 세 장소 모두 1행씩 배출(누락 없음)  
  - 수량 0은 숫자 `0`이 아니라 문자열 `"nan"`으로 기록 (업로더 무시 처리용)  
  - 시트2에 품목별 합계 (숫자)
- **🧭 실전 체험 가이드 (첫 진입 안내 + 5단계 체험 + 완료 팝업)**  
  - 첫 실행 시 먼저 안내 팝업이 뜨고, 시작 시 체크리스트와 가이드 팝업이 분리된 상태로 진행  
  - Home/Logs 이동 중에도 단계와 체크리스트가 유지되며, 완료 후에는 운영 주의사항 팝업을 표시  
  - 상단 `🧭 가이드` 버튼으로만 허브 재실행/복습 가능
- **📥 일괄 추가(베타)**  
  - `.xlsx`/`.csv`(첫 시트) 업로드  
  - **합산 모드**: 기존 수량에 가산  
  - **초기화 후 적용**: 스키마 0화 후 업로드 반영  
  - **로그는 생성하지 않음** (설계 의도)  
  - 파일 재업로드 시 또 증가하므로 주의  
  - 일부 동의어/오타 자동 보정

---

## 데이터 스키마
- **장소(3고정)**: `동아리방`, `비행장`, `교수님방`
- **계층 구조**: `장소 → 상위 → 하위 → (최하위) → 품목배열` (리프는 항상 배열)
- **카테고리 예시**
  - `공구`: `[수리, 납땜 용품, 드라이버, 그외 공구]`
  - `소모품`: 일부 3단 (`펜타 가드 → {새거, 중고, 기타}`, `테이프 → {필라멘트, 양면, 종이&마스킹, 절연, 그외 테이프}`)
  - `드론 제어부`: `[FC, ESC, 모터, …]`
  - `조종기 개수` / `기체 개수`: `[학교, 개인]`
- **Firebase 키 제약**  
  - 빈 문자열 / 금지문자 (`. # $ / [ ]`) 포함 키는 저장 시 제거됨

---

## 빠른 시작
> 환경: Ubuntu / Node.js 18+ 권장

```bash
# 클론
git clone https://github.com/x2okmiin/dokkaebi-inventory.git
cd dokkaebi-inventory

# 의존성 설치
npm install

# 환경변수 작성
printf "REACT_APP_VERSION=0.0.0-dev\n" > .env.local

# 개발 서버
npm start
````

접속: `http://localhost:3000`
로그인: 학번 + 이름 + 비밀번호 `2513` (관리자 모드 전환)

---

## Firebase 설정

`src/firebase.js`에 Firebase 프로젝트 설정이 이미 포함돼 있음.

* DB 구조:

  * `inventory/`: **set(전체 저장)**
  * `logs/`: **push/update/remove만**

---

## 빌드 & 배포

GitHub Pages 정적 배포 + 원클릭 스크립트(`scripts/ud`).

```bash
# 버전만 넘기면 scripts/ud가 package.json/.env.local/태그를 자동 갱신
npm run ud -- 1.6.0

# (선택) 커밋 메시지 직접 지정
npm run ud -- 1.6.0 "chore(release): v1.6.0"
```

* `.env.local`에 버전 기록 → 태그 생성 → 빌드 → gh-pages 푸시
* 실패 시 권한 부여: `chmod +x scripts/ud`

---

## 테스트 체크리스트

1. 수정 패널 버튼(＋/－/✎/📝) 동작, 카테고리 접힘 없음
2. 팝업 열자마자 `details` 전부 펼쳐짐
3. 반응형 그리드 (2×2, 3열+전체 가운데)
4. 검색 클릭 시 해당 위치 이동 + 집계 정확
5. Excel 내보내기: 시트1(재고현황, 0→nan), 시트2(품목별 합계)
6. 로그 페이지: 필터/내보내기/자동 병합 정상
7. 일괄 추가(베타): 합산·초기화 모드 모두 확인, 로그 미생성
8. Firebase 저장: 키 살균 경고만, 오류 없음
9. 모바일: 카드 스크롤/로그아웃 버튼 표시
10. 자동 로그아웃: 10분 무활동 시 동작|
11. beta.9 가이드: Home↔Logs 이동, 새로고침, 뒤로가기 후 step / checklist / coach 유지
12. beta.9 정책: completed 이후 skip 비노출, 재오픈 시 review 모드 유지

## 릴리즈 게이트

정식 `1.6.0` 전에는 아래 게이트를 반드시 다시 수동 확인한다. 상세 단계는 [`docs/RELEASE-CHECKLIST.md`](docs/RELEASE-CHECKLIST.md) 참고.

- Home↔Logs 반복 이동 후에도 guide runtime / checklist / coach가 초기화되지 않을 것
- completed 상태 이후 skip이 다시 상태를 덮어쓰지 않을 것
- 처음 쓰는 사람이 step 1~5를 설명 없이 따라갈 수 있을 정도로 피드백이 충분할 것
- 모바일에서 logs toolbar / guide dock / popup / toast가 겹치지 않을 것
- 일괄 추가 + 30분 백업 복구 + 자동 로그아웃 안내 + 첫 동기화 흐름이 회귀하지 않을 것

---

## 장애 · 디버깅

* **Firebase set 실패**: 빈키/금지문자 존재 → 저장 직전 살균
* **ESLint no-useless-escape**: 정규식 `const FORBIDDEN_KEY_RE = /[.#$/[\]]/;` 유지
* **모바일 스크롤/버튼 미표시**: `@media (max-width:960px)`에서 `.summary-grid .card-body{overflow:visible}` 오버라이드가 살아있는지 확인
* **팝업 접힘**: `requestAnimationFrame`으로 열자마자 `details.open=true` 강제
* **스플래시 미닫힘**: RTDB 지연 시 8초 후 강제 닫힘

---

## FAQ

* **일괄 추가 로그 안 남음?** → 의도된 설계 (재고만 변경)
* **같은 파일 또 올리면?** → 합산 모드라 계속 증가
* **수량 0은?** → Excel 내보내기 시 `"nan"` 처리 → 업로더 무시
* **관리자 비번?** → `2513`

---

## 운영 원칙

* 대규모 리팩터링 금지, 최소 변경 원칙
* 상태 불변성 유지 (`JSON.parse(JSON.stringify())` 패턴)
* 이벤트 전파 제어 (`preventDefault` + `stopPropagation`)
* RTDB 쓰기 최소화 정책 권장

---

## 변경 이력

**2026-03-23 · v1.6.0-beta.9**

* Home/Logs 가이드 진행 상태를 `GUIDE_RUNTIME_KEY` 단일 세션 흐름으로 재정리해 라우트 이동·새로고침·뒤로가기 이후에도 step/checklist/coach가 이어지도록 안정화
* 온보딩 상태를 `first-visit / prompted / in-progress / dismissed / skipped / completed / replay`로 명시하고 completed 이후 skip 금지, 재오픈 시 review-only 정책을 코드에 반영
* 각 단계 coach 문장을 더 직접적으로 수정하고 `가이드 대상` 배지, 힌트 박스, Logs checklist 존재감을 강화해 초보자 피드백을 보강
* 정식 1.6.0 직전 검증을 위해 release gate 문서(`docs/RELEASE-CHECKLIST.md`)를 추가하고 README/.welcome/package 버전을 beta.9로 동기화

**2026-03-18 · v1.6.0-beta.8**

* 첫 진입 UX를 테스트용 허브 중심 구조에서 분리해, 먼저 "처음 콘솔을 사용하시는군요" 안내 팝업을 띄운 뒤 사용자가 가이드 시작/스킵을 명확히 선택하도록 재설계
* 가이드를 총 5단계로 재구성하고, 체크리스트(진행 현황)와 코치 팝업(현재 해야 할 행동)을 다시 분리해 Home/Logs 어디서든 같은 흐름으로 이어지도록 정리
* 기록 페이지 이동 후에도 체크리스트가 함께 유지되도록 logs route 도킹 UI를 보강하고, 마지막 운영 체크포인트 step은 제거한 대신 완료 팝업 안에 운영 주의사항만 요약
* 권한/자동 로그아웃 단계는 별도 확인 버튼 대신 로그아웃 버튼 주변 hover·길게 누르기 기반 안내 박스로 변경해 실제 로그아웃 없이 정책 이해를 유도

**2026-03-16 · v1.6.0-beta.7**

* Logs 가이드 도킹 레이아웃을 compact 모드로 분리하고 logs route에 하단 여백을 부여해 체크리스트/코치가 로그 리스트를 가려 “막힌 화면”처럼 보이던 문제를 구조적으로 완화
* 가이드 런타임에 `resumeAllowed`/`closedByUser`/`completed` 상태를 추가해 “진행 중 새로고침만 자동 재개”, 닫기·스킵·완료 후 자동 재등장 차단 정책을 명시적으로 정리
* 4단계(권한/자동 로그아웃)에 실제 정책 안내 패널과 확인 액션을 추가하고, “로그아웃 버튼 위치 확인 + 정책 확인”이 모두 충족될 때만 완료 처리되도록 완료 조건을 보강
* 체크리스트/타겟 피드백을 `guideFeedback` 공통 상태로 통합해 단계 클릭 시 대상 스크롤 + 시각 강조(테두리/펄스) + 현재 항목 강조가 함께 동작하도록 개선


**2026-03-16 · v1.6.0-beta.6 hotfix**

* 가이드 오버레이 렌더링 지점을 `guide-floating-dock` 공통 슬롯으로 통합해 Home/Logs에서 동일한 mount 책임을 사용하도록 정리하고, Logs 페이지 코치가 화면 밖으로 밀리는 문제를 구조적으로 수정
* Logs 가이드 강제 보장 effect에 1회 준비 가드를 추가하고 runtime 동등성 비교를 도입해 동일 step 상태를 반복 저장하던 루프성 갱신(중복 팝업 체감 원인)을 차단
* 첫 진입 시 허브 팝업(`tutorialOpen`)과 코치(`guidedTutorialOpen`)가 동시에 노출되지 않도록 초기 복구 정책을 정리해 단계 중복 노출을 완화
* 단계별 하이라이트 피드백을 `guide-highlight-target`으로 통합하고, 테스트 잔재였던 `.tutorial-target-pulse` 의존을 제거
* 장소/전체 카드 확대보기 헤더 버튼의 커서를 `pointer`로 명확화하고 hover/focus-visible 인터랙션 피드백을 강화


**2026-03-16 · v1.6.0-beta.6**

* 가이드 상태 전이를 beta.6 컨트롤러 유틸(`buildStepStatesWithDone`, `buildAdvancedRuntime`)로 통합해 Home/Logs 간 step 진행 책임 충돌을 줄임
* Logs step 2 완료 후 Home 복귀 시 step 3(검색 이동)로 이어지도록 back/popstate/다음단계 저장 로직을 단일 정책으로 정리
* Home 자동 액션에서 Logs 전용 step(2단계)을 재실행하지 않도록 차단해 `/logs` 재진입 루프와 돌아가기 먹통 현상을 완화
* 체크리스트 패널/가이드 코치/토스트를 `guide-floating-dock` 슬롯 레이아웃으로 재배치해 상호 겹침을 구조적으로 방지
* 스토리지 키/문서/패키지 버전을 `1.6.0-beta.6`으로 동기화하고 기존 runtime 키는 유지해 이어하기 호환성을 보존


**2026-03-16 · v1.6.0-beta.5**

* beta.4 가이드 런타임을 안정화해 로그 2단계 완료 조건을 단일 처리 함수로 통합(필터 토글/내보내기 메뉴 열기/CSV·Excel 실행)하고, 완료 후 다음 단계 이동 신뢰성을 개선
* "이번만 스킵" 상태를 자동 시작 차단 플래그로 명시화해 새로고침/라우팅 이동/뒤로가기 후 가이드 자동 재등장 버그를 방지 (수동 `🧭 가이드` 재오픈은 유지)
* 하단 가이드 코치와 토스트 배치를 화면 크기별로 분리(데스크탑 좌하단+우하단, 모바일 하단+상단)하여 버튼 가림/위치 충돌 UX를 해소
* 문서/가이드 표기/스토리지 키/패키지 버전을 `1.6.0-beta.5`로 동기화


**2026-03-16 · v1.6.0-beta.4**

* 실전 체험 가이드를 시작 허브/체크리스트 패널/하단 코치 바 3층 구조로 재구성
* 6단계에 실제 완료 조건(카드 열기, 로그 필터·내보내기 조작, 검색 결과 클릭, 권한 체크, 데이터 메뉴 열기, 운영 체크 확인)을 연결
* 단계별 상태(`idle`/`waiting-action`/`done`/`skipped`)와 pending action을 session/local storage에 저장해 이어하기 안정성을 강화
* 로그 페이지 필터를 자동 상태(`autoCollapsed`)와 사용자 상태(`userFilterExpanded`)로 분리해 버튼 무반응 충돌 버그를 해결
* 수동 펼침 직후 자동 접힘 재개를 지연시키는 쿨다운을 도입해 모바일/태블릿에서 필터 토글 신뢰성을 개선


**2026-03-16 · v1.6.0-beta.3**

* 체험형 가이드 상태를 런타임(sessionStorage)과 완료 상태(localStorage)로 분리해 해시 라우팅 전환 시 단계 씹힘/초기화 문제를 보정
* 첫 강제 진입 규칙 정리: 처음에는 닫기 숨김, 사용자가 실제 체험 시작 후 닫기 허용, 닫기 가능 상태에서는 스킵 비활성화
* 로그 페이지에서도 2단계 가이드 코치가 이어지도록 연결해 페이지 이동 중 하단 가이드 팝업 단절 해소
* 모바일/태블릿 로그 필터 접힘 로직 보강(스크롤 자동 축소 + 수동 토글/회전/리사이즈 충돌 완화)
* .welcome.sh를 인수인계 중심 운영 가이드로 확장하고 점검 키(local/session storage) 안내를 명시

**2026-03-14 · v1.6.0-beta.2**

* 모바일 카드/하위 카테고리 탐색 시 페이지 스크롤 우선 동작으로 조정하고, 긴 콘텐츠에서만 내부 스크롤이 동작하도록 UX 재설계
* 로그 페이지 필터를 모바일 접힘 UI(자동 축소 + 수동 토글)로 개편해 기록 리스트 가시성 개선
* 가이드를 6단계 체험형 온보딩으로 확장하고 단계별 화면 이동/페이지 이동 유도를 추가
* 모바일 하단 safe-area 기준으로 가이드 코치 팝업 위치를 보정하고 토스트와 z-index 충돌 정리

**2026-03-14 · v1.6.0-beta.1**

* 첫 실행 기기 대상 통합 온보딩 가이드(자동 진입 + 스킵 가능 + 상태 저장)로 교체
* 스플래시/부트/RTDB 동기화 완료 이벤트 흐름을 단일화해 중복 로딩 화면 노출 문제 정리
* .welcome.sh를 운영/인수인계 중심 실무 가이드로 전면 개편
* Firebase 래퍼에 디버깅/성능 로그 포인트를 추가해 후속 최적화 준비

**2026-03-14 · v1.5.9-beta.7**

* UI 안정화 및 베타 테스트 마감 정리
* 데이터 메뉴에서 일괄 추가 실행 전 현재 재고를 30분 동안 로컬 백업하고, 필요 시 백업 복구가 가능하도록 정리
* 메인 화면에서 검색 결과 더보기 지원, 결과 클릭 시 해당 품목으로 스크롤 이동 및 하이라이트 표시
* 로그인 화면에 학번 9자리 검증 적용, 비밀번호 입력 시 모바일 숫자 키패드 유도
* 새 기기 첫 접속 시 최신 RTDB 데이터 수신 전까지 부트 로딩 상태를 유지하고, 지연 시 폴백 흐름 추가
* 첫 사용자용 빠른 가이드 및 일괄 추가 체험형 튜토리얼 추가 (v1.6.0-beta에서 통합 온보딩으로 재구성 예정)
* 모바일 로그 화면 툴바 접힘 스타일 및 스플래시 보조 텍스트 구조 보강
* v1.6.0 본패치 전 기준선 정리 및 문서 정비

**2025-08-26 · v1.5.9**

* Excel 내보내기: 수량 0 → `"nan"` 문자열로 기록
* 일괄 추가(베타): 초기화 모드 + 합산 모드 정식 반영
* 버전 라벨: 더블클릭으로 로컬 수정 가능
* 스플래시: RTDB 지연 시 8초 폴백 닫기
* .welcome.sh 배너 추가 (버전/커밋 표시, 초기 셋업 가이드 포함)

---